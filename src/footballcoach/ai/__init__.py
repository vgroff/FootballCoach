"""Neural-network / PPO player AI for Football Coach.

See ai_design_doc.md for the full architecture specification.

This package is a pure consumer of the engine (Match, orders, actions,
entities, config) - it does not touch or modify the engine package.

Install the AI dependencies with:
    uv sync --group ai
"""
