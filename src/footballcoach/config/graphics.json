{
  "_comment": "Visual display constants. Edit freely to tune the look without touching code. See ui/knowledge.md for details on how each value is used.",
  "target_fps": 60,
  "_comment_target_fps": "Render frame rate cap. Physics ticks are governed separately by gameplay.json ui.physics_tick_hz.",
  "camera": {
    "_comment": "pixels_per_metre: zoom level. 9=1025x692 window, 12=1340x896, 14=1570x1032. margin_px: empty border around pitch.",
    "pixels_per_metre": 13.0,
    "margin_px": 50
  },
  "player": {
    "_comment": "min_radius_px: smallest on-screen circle radius drawn for a player. Physical world positions are unaffected — only the rendered dot size is boosted for visibility. possession_outline_thickness: pixel width of the white ring drawn around the ball carrier (1=thin, 3=bold). inactive_alpha: opacity (0-255) of a player drawn translucent after being tackled (0=invisible, 255=fully opaque).",
    "min_radius_px": 7,
    "possession_outline_thickness": 1,
    "inactive_alpha": 190
  },
  "ball": {
    "_comment": "min_radius_px: smallest on-screen circle radius drawn for the ball. outline: whether to draw a dark border around the ball circle. outline_width_px: pixel width of that border (1=thin, 3=bold). height_boost_per_metre: how much the drawn ball radius grows per metre of height (0=no growth, 0.35=original exaggerated, 0.1=subtle).",
    "min_radius_px": 5.5,
    "outline": true,
    "outline_width_px": 0.66,
    "height_boost_per_metre": 0.1
  },
  "action_icons": {
    "_comment": "linger_s: wall-clock seconds an action icon stays visible above a player after the action fires. font_size_px: point size of the icon font — keep small (10-14) or they'll dominate the screen.",
    "linger_s": 1.5,
    "font_size_px": 18
  },
  "speed_lines": {
    "_comment": "threshold_mps: player ground speed above which trailing speed lines are drawn behind the player. count: number of parallel lines. length_px: length of each line in pixels. gap_px: spacing between successive lines.",
    "threshold_mps": 7.0,
    "count": 3,
    "length_px": 4.5,
    "gap_px": 2
  },
  "stamina_flash": {
    "_comment": "threshold: stamina fraction (0-1) below which a flashing warning outline pulses around the player. flash_hz: flashes per second (wall-clock).",
    "threshold": 0.3,
    "flash_hz": 2.5
  },
  "heading_indicator": {
    "_comment": "Short line showing which direction a player is facing. length_px: how far the tip extends beyond the player circle edge. alpha: 0=invisible, 255=fully opaque. Set alpha to 0 to hide entirely.",
    "length_px": 4,
    "alpha": 60
  },
  "kick_ui": {
    "_comment": "Kick trajectory preview and spin input constants. max_drag_m: mouse distance (metres) for full power — also scales elevation and spin sensitivity. trajectory_duration_s: seconds of trajectory shown. max_loft_angle_deg: elevation at closest mouse position. max_spin_magnitude_rads: spin at furthest mouse position.",
    "max_drag_m": 10.0,
    "trajectory_duration_s": 0.8,
    "max_loft_angle_deg": 70.0,
    "max_spin_magnitude_rads": 15.0
  },
  "pause_notification": {
    "_comment": "font_size_px: point size of the auto-pause banner rendered when a human order completes. Default title_font is 32; 26 = ~20% smaller.",
    "font_size_px": 26
  },
  "ball_spin_dots": {
    "_comment": "Dots on the 3D ball surface (Fibonacci sphere), always shown, rotating with ball spin. Only front-hemisphere dots (facing camera) are drawn — back hemisphere is hidden. count: number of dots. projection_scale_fraction: how wide the projected sphere appears relative to ball drawn radius (1.0 = dots reach ball edge). dot_radius_fraction: dot radius / ball drawn radius. color: RGB.",
    "count": 10,
    "projection_scale_fraction": 0.98,
    "dot_radius_fraction": 0.33,
    "color": [30, 30, 30]
  },
  "ball_state_rings": {
    "_comment": "Coloured indicator rings drawn around the ball to show its state. offset_px: how far outside the ball circle the ring sits. width_px: ring line width. Set show_flying/show_rolling/show_bounced to false to hide individual rings. Colors are [R,G,B]. bounce_duration_s: how long the amber bounce ring stays visible after a bounce (also controls physics.json just_bounced_display_duration_s if you want them in sync — but this one only affects the renderer display; to change the underlying timer duration edit physics.json). flying_min_height_above_ground_m: margin above ball.radius_m at which the flying ring activates (0.01 = only genuinely airborne).",
    "show_flying": true,
    "show_rolling": true,
    "show_bounced": true,
    "flying_min_height_above_ground_m": 0.01,
    "offset_px": 2,
    "width_px": 2,
    "color_flying": [180, 230, 255],
    "color_rolling": [180, 255, 180],
    "color_bounced": [255, 210, 80]
  },
  "ball_trail": {
    "_comment": "Fading ghost trail behind moving ball. length: number of ghost positions stored (sampled at physics rate). min_speed_mps: no trail below this ground speed. max_alpha: alpha of the newest ghost position. radius_fraction: ghost circle radius relative to ball drawn radius. radius_taper: how much the radius shrinks toward the oldest ghost (0=no shrink, 0.35=oldest is 35% smaller than newest, 1.0=oldest shrinks to nothing). interp_steps: sub-samples inserted between each stored position when drawing (1=no interpolation, 3=two extra sub-points per gap).",
    "length": 7,
    "min_speed_mps": 2.0,
    "max_alpha": 80,
    "radius_fraction": 0.9,
    "radius_taper": 0.85,
    "interp_steps": 2
  }
}
